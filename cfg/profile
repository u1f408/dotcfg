[ -d "$HOME/.local/bin" ] && PATH="$HOME/.local/bin:$PATH"
[ -d "$HOME/bin" ] && PATH="$HOME/bin:$PATH"
export PATH

_EDITORS="mle nvim vim vi"
for _ed in ${_EDITORS}; do
	if command -v "${_ed}" >/dev/null; then
		EDITOR="${_ed}"
		break
	fi
done
unset _EDITORS _ed
export EDITOR
